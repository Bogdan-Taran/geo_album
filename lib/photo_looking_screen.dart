import 'package:flutter/material.dart';

class PhotoLookingScreen extends StatelessWidget {
  const PhotoLookingScreen({super.key});

  @override
  Widget build(BuildContext context){
    return const Center(
      child: Text('Здесь экран просмотра фотографии'),
    );
  }
}